import { ModifyCountPipe } from './modify-count.pipe';

describe('ModifyCountPipe', () => {
  it('create an instance', () => {
    const pipe = new ModifyCountPipe();
    expect(pipe).toBeTruthy();
  });
});
