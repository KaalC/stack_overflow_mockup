import { ModifyDatePipe } from './modify-date.pipe';

describe('ModifyDatePipe', () => {
  it('create an instance', () => {
    const pipe = new ModifyDatePipe();
    expect(pipe).toBeTruthy();
  });
});
